export interface OrderInterface {
    id: number;
    orderDate: string,
    customerId: number,
    productId: number,
    quantity: number,
    price: number,
    currency: string,
    date: string
}